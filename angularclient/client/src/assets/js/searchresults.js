function setSearchResultsGridWidth()
{
    //$("#search-results").css("width", "auto");
    //var totalWidth = $(document).width();
    //var thumbnailWidth = $("#search-results span").outerWidth(true);
    //var thumbsPerRow = Math.floor(totalWidth / thumbnailWidth);
    //$("#search-results").width(thumbsPerRow * thumbnailWidth);
}